import React from 'react'
import "./Loader.css";

const Loader = () => {
    return (
        <div className="loader">
          {/* You can use a spinner or any other animation */}
          <div className="spinner"></div>
        </div>
      );
}

export default Loader